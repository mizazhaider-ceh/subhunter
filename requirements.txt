# SubHunter v5.0 Dependencies
# Core (required)
httpx>=0.24.0
aiodns>=3.0.0

# Optional: Screenshots (choose one)
# playwright>=1.40.0      # Recommended: pip install playwright && playwright install chromium
# selenium>=4.15.0         # Fallback:   pip install selenium webdriver-manager
# webdriver-manager>=4.0.0
